// swift-tools-version:4.0

import PackageDescription

let package = Package(
    name: "Validator",
    targets: [
        .target(
            name: "Validator",
            dependencies: []),
    ]
)
